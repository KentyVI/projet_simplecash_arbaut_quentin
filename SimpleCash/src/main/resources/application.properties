# ------------
# Datasource H2 (par défaut)
# ------------
spring.datasource.url=jdbc:h2:file:./build/h2/simplecash
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect


# Console H2 pratique
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ------------
# Exemple MySQL (désactivé)
# ------------
# spring.datasource.url=jdbc:mysql://localhost:3306/kenty_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=kenty
# spring.datasource.password=050402
# spring.jpa.hibernate.ddl-auto=update
# app.initdb=true

# Logging levels: moins de bruit framework, plus pour l'app
logging.level.root=INFO
logging.level.org.springframework=WARN
logging.level.org.hibernate=WARN
logging.level.com.example.simplecash=INFO

# Config Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.packages-to-scan=com.example.simplecash.controller
